description: suite1
maintainers:
  - maint1
cases:
    - name: tarball
      max_duration_seconds: 600
      pattern:
          location_types:
              or:
                  - tarball-path
                  - tarball-url
    - name: rpm
      max_duration_seconds: 600
      pattern:
          location_types:
              or:
                  - rpm-path
                  - rpm-url
    - name: repo
      max_duration_seconds: 600
      pattern:
          location_types:
              or:
                  - repo-path
                  - repo-url
