description: suite1
cases:
    - name: tarball
      match:
          location_types:
              - tarball-path
              - tarball-url
    - name: rpm
      match:
          location_types:
              - rpm-path
              - rpm-url
    - name: repo
      match:
          location_types:
              - repo-path
              - repo-url
